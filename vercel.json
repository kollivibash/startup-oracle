{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "buildCommand": "rm -rf node_modules/.vite node_modules/.cache .vercel/cache dist && npm run build",
  "outputDirectory": "dist"
}
